{
  "name": "lua-build-watch",
  "version": "1.0.0",
  "description": "A build and watch script for Lua files with bundling and minification.",
  "main": "build.ts",
  "scripts": {
    "build": "tsx build.ts",
    "watch": "tsx build.ts --watch"
  },
  "keywords": ["lua", "build", "watch", "bundle", "minify"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "chokidar": "latest",
    "luabundle": "latest", 
    "luamin": "latest"
  },
  "devDependencies": {
    "@types/chokidar": "latest",
    "@types/node": "^22"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
